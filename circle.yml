machine:
  node:
    version:
      0.12.0
test:
  pre:
    - npm run start-webdriver:
        background: true
    - npm run start-server:
        background: true
    - sleep 5
  override:
    - npm run protractor
