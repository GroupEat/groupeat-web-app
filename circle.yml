machine:
  node:
    version:
      0.12.0
test:
  pre:
    - npm update-webdriver
    - npm start-webdriver:
        background: true
    - npm start-server:
        background: true
    - sleep 5
  override:
    - npm run protractor
